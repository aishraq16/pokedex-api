# Pokedex API

A simple Express-based API for fetching Pokémon information.

## Getting started

Install dependencies:

```sh
npm install
```

Start the server with watch mode:

```sh
node.js --watch app.js
```

The API will be available at:

- GET /pokemon/:name
- GET /pokemon?type=...&region=...
